# SOURCE PASSPORT — РНП / ГИС ЕИС ЗАКУПКИ

Дата фиксации: 15.09.2026
Статус: официальный машинный доступ подтверждён; интеграция временно ожидает завершения доступа к ЕИС. Сторонний API DaMIA не подключаем и не используем как источник базы.

Дополнительные обязательные ограничения по приватности и нагрузке: `docs/RNP_EIS_PRIVACY_LOAD_POLICY.md`.

## 1. Источник

- Система: ГИС ЕИС ЗАКУПКИ.
- Официальный сайт: https://zakupki.gov.ru/
- Оператор ЕИС: Федеральное казначейство.
- Реестр: Реестр недобросовестных поставщиков (подрядчиков, исполнителей), прежде всего РНП по 44-ФЗ; после live-подтверждения 44-ФЗ отдельно проверяется РНП по 223-ФЗ.
- Source Registry code: `eis`.

## 2. Правовое и техническое основание доступа

С 01.01.2025 прежний FTP-канал ЕИС не является рабочим способом получения машиночитаемых данных.

Постановление Правительства РФ от 09.12.2024 № 1740 изменило Положение о ЕИС, утверждённое постановлением Правительства РФ от 27.01.2022 № 60. Для получения информации и документов, размещённых в форме открытых данных в машиночитаемом виде, требуется идентификация, аутентификация и авторизация в ЕИС.

Официальные сервисы отдачи:

- `getDocsIP` — для физических лиц;
- `getDocsLE` — для юридических лиц.

Для работы требуется регистрация получателя машиночитаемых данных с использованием ЕСИА.

Официальная документация ЕИС:

- раздел документов: https://zakupki.gov.ru/epz/main/public/document/view.html?searchString=&sectionId=432&strictEqual=false
- инструкция по сервисам отдачи: https://zakupki.gov.ru/epz/main/public/download/downloadDocument.html?id=41500

## 3. Выбранный режим для проекта

Режим: **AUTHENTICATED OFFICIAL SOAP / DOCUMENT DELIVERY**.

На первом этапе используем `getDocsIP`, если доступ оформлен на физическое лицо/ИП. Для юридического лица архитектура должна позволять заменить transport/auth на `getDocsLE` без изменения доменной модели РНП.

Точка доступа физлица:

`https://int44.zakupki.gov.ru/eis-integration/services/getDocsIP`

Namespace SOAP:

`http://zakupki.gov.ru/fz44/get-docs-ip/ws`

Для РНП по 44-ФЗ подтверждается целевой subsystem `RNP`. В интеграционных примерах текущего сервиса для массового запроса по региону/дате используется документ `unfairSupplier2022`. Точный действующий контракт полей и XML внутри возвращаемого архива фиксируем только после live smoke-test с нашим токеном и фактическим XSD/ответом ЕИС.

## 4. Почему не используем другие способы

Не использовать:

- закрытый с 01.01.2025 FTP ЕИС;
- scraping HTML-поиска РНП;
- обход anti-bot/ограничений сайта;
- сторонний API как постоянный source of truth.

Публичная HTML-часть остаётся полезной для ручной проверки, но не является нашим рабочим ingestion-каналом.

### DaMIA

DaMIA зарегистрирована/изучена как возможный резервный bridge через `API-Закупки`, но **сейчас не подключается**.

Решение:

- не строить базу РНП на DaMIA;
- не покупать/не подключать DaMIA ради временного обхода текущего blocker ЕИС;
- оставить DaMIA только как резервный вариант для разработки/fallback, если официальный доступ ЕИС затянется или станет практически недоступен;
- даже при временном использовании DaMIA данные должны нормализоваться в нашу собственную модель РНП, чтобы позже заменить transport на ЕИС без переделки продуктового слоя;
- официальный ЕИС остаётся источником истины.

## 5. Секреты и безопасность

Токен `getDocsIP` хранится только локально:

`EIS_IP_TOKEN=<token>`

`.env` уже исключён из Git через `.gitignore`.

Запрещено:

- коммитить token в GitHub;
- печатать token в логах;
- сохранять token в ingestion details;
- передавать token в URL query string;
- прикреплять `.env` в чат или issue/PR.

При скачивании сформированного архива токен также передаётся только в HTTP header, как требует сервис.

## 6. Product semantics

Наличие записи в РНП — самостоятельный существенный факт риска и должно показываться с:

- основанием включения;
- номером записи;
- законом/контуром (44-ФЗ / 223-ФЗ);
- датой включения;
- плановой/фактической датой исключения;
- текущим статусом записи;
- источником и датой данных.

Отсутствие записи можно трактовать только в рамках реально загруженного/проверенного покрытия ЕИС. Ошибка доступа, отсутствие токена, ошибка SOAP или недоступность ЕИС никогда не превращаются в `not_found`.

Связь с Master Registry — по ИНН/ОГРН/ОГРНИП из официальной записи. Название и ФИО не используются как автоматический ключ.

### Privacy rule

Персональные данные физических лиц из РНП не публикуются в публичной карточке, публичном API, SEO, выгрузках и поисковом индексе. Если такие поля не нужны для строгого matching организации, не сохраняем их в нормализованной продуктовой модели. Подробности — `docs/RNP_EIS_PRIVACY_LOAD_POLICY.md`.

### Load rule

До появления явных официальных лимитов работаем в режиме LOW LOAD: один сетевой worker, один запрос одновременно, стартовый интервал между SOAP-запросами не менее 30 секунд, локальный cache/archive reuse, exponential backoff и остановка цикла при защитных ошибках/rate-limit. Подробности — `docs/RNP_EIS_PRIVACY_LOAD_POLICY.md`.

## 7. Текущее решение по Phase 3

Источник №12 **остаётся незавершённым и не заменяется DaMIA**.

Что подтверждено:

- официальный допустимый машинный канал ЕИС существует;
- credential-safe provider/live-probe реализован и прошёл CI;
- регистрация через ЕСИА начата;
- ЕИС запросила недостающие сведения ЕГРИП из ФНС и указала срок до 5 рабочих дней;
- для доступа в личный кабинет потребителя машиночитаемых данных ЕИС также показала требование квалифицированного сертификата ключа проверки электронной подписи.

Текущий статус: **внешний blocker / WAITING FOR EIS ACCESS**.

До снятия blocker:

- DaMIA не интегрируем;
- новую временную базу РНП не создаём;
- Фазу 4 автоматически не начинаем;
- возвращаемся к №12, когда станет доступен официальный credential/token или будет отдельно принято решение использовать резервный bridge.

До получения token в коде допустимы:

- request builders;
- response parser;
- credential-safe provider;
- offline unit tests;
- probe CLI, который отказывается обращаться в сеть без token.

Полноценный ingestion/model не фиксируется до первого реального архива РНП и инспекции его XML.

## 8. Definition of Done для №12

1. Регистрация получателя машиночитаемых данных в ЕИС.
2. Реальный `getDocsIP`/`getDocsLE` smoke-test.
3. Получение официального архива РНП.
4. Инспекция XML/XSD и фиксация точного parser contract.
5. Model + migration + ingestion history + idempotent import.
6. Строгий matching с Master Registry.
7. Coverage-аудит и semantics `found / not_found / unavailable`.
8. Service → aggregator → UI/API без публикации персональных данных физлиц из РНП.
9. LOW LOAD / cache / backoff protections.
10. Tests + CI.
11. Реальная карточка компании с записью РНП.
12. Отдельное решение/проверка покрытия РНП 223-ФЗ.
13. Обновление `PROJECT_STATUS.md` и закрытие Фазы 3 по согласованному scope.
