<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for row in rows %}<url><loc>{{ origin }}/companies/{{ row.inn }}</loc><lastmod>{{ row.content_updated_at.date().isoformat() }}</lastmod></url>
{% endfor %}</urlset>
